assert_lefthook_installed: true
colors: false
source_dir: scripts/

commit-msg:
  commands:
    'lint commit message':
      run: pnpm exec commitlint --edit {1}

pre-commit:
  skip:
    - merge
    - rebase

  commands:
    test:
      run: pnpm run test
